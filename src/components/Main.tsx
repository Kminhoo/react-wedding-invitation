import React from 'react'
import Hero from '@sections/Hero'

const Main = ({ children }: { children: React.ReactNode }) => {
  return (
    <main>
      <Hero />
    </main>
  )
}

export default Main
